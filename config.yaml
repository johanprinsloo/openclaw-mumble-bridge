mumble:
  host: "192.168.1.100"
  port: 64738
  username: "OpenClaw"
  password: ""
  channel: "General"
  certfile: null

stt:
  provider: "openai"
  openai_api_key: "${OPENAI_API_KEY}"
  language: "en"
  model: "whisper-1"

openclaw:
  gateway_url: "http://127.0.0.1:18789"
  gateway_token: "${OPENCLAW_GATEWAY_TOKEN}"
  agent_id: "main"
  session_user: "mumble-room"
  timeout_seconds: 60

tts:
  provider: "openai"
  openai_api_key: "${OPENAI_API_KEY}"
  voice: "nova"
  speed: 1.0
  # elevenlabs_api_key: "${ELEVENLABS_API_KEY}"
  # elevenlabs_voice_id: "pMsXgVXv3BLzUgSXRplE"

bridge:
  max_concurrent_utterances: 2
  log_level: "INFO"
